import React, { Component } from 'react';
import {
  Platform,
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity
} from 'react-native';

export default class HomeRefreshFooter extends Component {
  render() {
    return (
      <View style={styles.container}>
      
      </View>
    );
  }
}

var styles = StyleSheet.create({
  container: {
    height: countcoordinatesY(45),
    width: ScreenWidth,
    backgroundColor: ThinBGColor,
    alignItems: 'center',
    justifyContent: 'center'
  },
})